{
  "name": "caiguo-ledger",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "android:prepare": "python3 scripts/prepare_android.py",
    "android:sync": "npm run android:prepare && npx cap sync android",
    "android:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/core": "^7.4.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3"
  }
}
